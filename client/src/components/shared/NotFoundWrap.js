import React from 'react';
import NotFound from './NotFound';
import { BodyWrapper, MainSection } from './BodyMain';

const NotFoundWrap = () => (
  <BodyWrapper>
    <MainSection>
      <NotFound />
    </MainSection>
  </BodyWrapper>
);

export default NotFoundWrap;
